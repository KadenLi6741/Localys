import { ManagerPlaceholder } from '../_components/Placeholder';

export default function ManagerAnalytics() {
  return <ManagerPlaceholder title="Analytics" description="Views, visits, likes, orders, reviews and advertise ROI over time." />;
}
