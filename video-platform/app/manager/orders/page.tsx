import { ManagerPlaceholder } from '../_components/Placeholder';

export default function ManagerOrders() {
  return <ManagerPlaceholder title="Orders" description="Incoming orders (Stripe test mode) with demo/seeded data." />;
}
