import { ManagerPlaceholder } from '../_components/Placeholder';

export default function ManagerPayments() {
  return <ManagerPlaceholder title="Payments" description="Payouts and payment settings (Stripe test mode)." />;
}
